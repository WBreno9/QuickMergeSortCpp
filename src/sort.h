#ifndef SORT_H
#define SORT_H

#include <vector>
#include <algorithm>
#include <fstream>
#include <cstring>

void mergeSort(std::vector<int64_t>& v);
void quickSort(std::vector<int64_t>& v);

#endif // !SORT_H